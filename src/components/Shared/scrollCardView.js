import React from "react";
import{
    View,ScrollView,Text
}from 'react-native';
import LinearGradient from "react-native-linear-gradient";

const ScrollCardView = ({}) => {
    return(
        <ScrollView>
            
        </ScrollView>
    )
}

export default ScrollCardView;